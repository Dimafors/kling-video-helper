import streamlit as st

st.set_page_config(page_title="🎬 Бесплатный ИИ-видео редактор", layout="centered")
st.title("🎭 Генератор видео из фото (100% бесплатно)")
st.markdown("Загрузите фото и опишите движение — мы подготовим всё для **Kling AI**!")

uploaded_file = st.file_uploader("📸 Загрузите изображение", type=["jpg", "jpeg", "png"])

if uploaded_file:
    st.image(uploaded_file, caption="Ваше изображение", use_column_width=True)

    motion = st.text_area(
        "✍️ Опишите движение (на английском!)",
        placeholder="Например: 'The camera slowly zooms in on the face'",
        height=100
    )

    if st.button("🚀 Перейти в Kling AI", type="primary"):
        if not motion.strip():
            st.warning("Пожалуйста, опишите движение.")
        else:
            st.success("✅ Готово! Следуйте инструкции ниже.")
            st.markdown("### 🔗 Как получить видео:")
            st.markdown("""
            1. Откройте [Kling AI](https://kling.ai)
            2. Нажмите **«Image to Video»**
            3. Загрузите это же изображение
            4. Вставьте ваш текст движения:
            """)
            st.code(motion)
            st.markdown("5. Нажмите **Generate** → дождитесь видео → скачайте!")
            st.info("💡 **Kling AI полностью бесплатен** (требуется регистрация).")
else:
    st.info("ℹ️ Поддержка до **10 секунд видео**, высокая реалистичность — и всё **бесплатно**!")
